import { CrossOutDirective } from './cross-out.directive';

describe('CrossOutDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossOutDirective();
    expect(directive).toBeTruthy();
  });
});
