import { WidthChangeDirective } from './width-change.directive';

describe('WidthChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new WidthChangeDirective();
    expect(directive).toBeTruthy();
  });
});
